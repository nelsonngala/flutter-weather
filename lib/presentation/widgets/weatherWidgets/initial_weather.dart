import 'package:flutter/material.dart';

class InitialWeather extends StatelessWidget {
  const InitialWeather({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tap the search icon in top right to search weather.'),
    );
  }
}
